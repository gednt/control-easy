---
version: 1
slug: "c-app-layout-shell-app-shell-component-ts-21b85c4b"
primary_target: "src/Web/ControlEasyReborn.Web/src/app/layout/shell/app-shell.component.ts"
related_targets: ["src/Web/ControlEasyReborn.Web/src/app"]
---

# Shift Ledger — authenticated application

Mode: Operate

Scope: Replace the visual system of the authenticated Angular application for every existing role and route while preserving all capabilities, routes, role guards, content meaning, and working behavior.

Audience and job: Administrators, gatehouse attendants, and residents must find the current work, its ownership, and its next action without decoding a generic dashboard.

Constraints: Operational clarity is the priority. Keep all current routes and functional capabilities intact. Build path: code-first.

## Direction contract

THESIS: The product is a trustworthy shift handover, not a card dashboard; every record makes its current state, responsible role, and next action legible.

OWN-WORLD: Charcoal framing holds warm ledger-paper workspaces, ruled rows, tab dividers, restrained brick-red exception stamps, muted brass priority marks, and precise slate-blue metadata; controls inherit the record-and-handoff language rather than generic panels.

STORY: On every route, a user can identify what is active, what needs attention, where it belongs, and what action closes or advances it; routine work stays quiet while exceptions visibly interrupt the record.

FIRST VIEWPORT: The authenticated dashboard opens as a three-part shift spread: a slim dark navigation rail at left, a wide current-work ledger with live arrivals and operational totals at center, and a resident/apartment lookup plus handoff timeline at right; the primary entry action is a stamped control above the active queue.

FORM: The Shift Ledger is the first-ranked grounded direction, selected as IMPECCABLE’S PICK from direction seed `69ef781a`; its signature interaction is a continuous handoff where a resolved row travels from the active ledger into the chronological handover trail while the next accountable item becomes prominent.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
