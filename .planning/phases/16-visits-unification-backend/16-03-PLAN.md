---
phase: 16-visits-unification-backend
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Abstractions/IReportReadRepository.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Contracts/ReportDtos.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Handlers/GetHistoryHandler.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/Persistence/ReportReadRepository.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/Endpoints/ReportEndpoints.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/DI/ReportEndpointsExtensions.cs
  - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/DI/ReportsModuleServiceCollectionExtensions.cs
  - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Contracts/PhotoDtos.cs
  - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Handlers/EntryLogHandlers.cs
  - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Validators/EntryLogRequestValidator.cs
  - tests/ControlEasyReborn.IntegrationTests/ReportEndpointTests.cs
  - tests/ControlEasyReborn.IntegrationTests/EntryLogEndpointTests.cs
autonomous: true
requirements: [VISIT-02, VISIT-04, VISIT-05, VISIT-06, VISIT-07]

estimate:
  tokens: 65000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - GET /api/v1/reports/history returns ONE chronological stream where each real-world entry appears exactly once — a QR-scan visitor arrival yields the Visit row OR the AccessEvent context row depending on the double-count rules, never both counted as separate entries
    - Every ledger row carries a kind discriminator (visit / access-event / legacy-entry-log), its native state stamp (VisitStatus for visits, AccessDecision/PolicyOutcome for events, consent EntryState for legacy rows), and a source origin label
    - Refused scans and security events appear as context rows in their native decision state — never coerced into Visit status enums (per D-03)
    - Server-side pagination with page/pageSize and X-Total-Count header (the header pattern the Phase 13 deferred-shim decision anticipated)
    - Every UNION branch predicates tenant_id = @p0 explicitly — TenantFilterInterceptor never splices into a UNION (per D-03/STATE.md)
    - Legacy ConsentAuditLog walk-in rows below the build-constant cutoff appear as the read-only legacy-entry-log segment; rows at/after the cutoff do NOT appear
    - carrierCode and description filters work server-side on the ledger; apartment-bound package rows are filterable by apartment
  artifacts:
    - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Handlers/GetHistoryHandler.cs
    - Unified ledger response contract in ReportDtos.cs (HistoryRowResponse with kind discriminator)
  key_links:
    - GetHistoryHandler → ReportReadRepository single SQL UNION (Visits ∪ AccessEvents ∪ ConsentAuditLog-cutoff) with per-branch tenant predicate
    - TenantDayBoundary (plan 16-01) → GetHistoryHandler date-range normalization (from/to resolve via day boundaries, not naive UTC)
    - AccessEvents.EventKind=PackageDrop rows (plan 16-02) → ledger rows with carrierCode/description fields
---

<objective>
Build the unified honest ledger: GET /api/v1/reports/history reads Visits + AccessEvents + the legacy ConsentAuditLog cutoff segment as one chronological, tenant-predicated, server-side paginated stream with native state stamps, kind discriminators, source labels, and package/carrier filters — and re-scope POST /api/v1/entry-log to its consent-only role.

Purpose: This is the read-model rewrite that closes the double-counting window (STATE.md decision: write fold + read rewrite in the same phase). The dashboard's current ledger double-adds ConsentAuditLog rows; from this phase the honest stream is /reports/history.

Output: HistoryRow contract, GetHistoryHandler, rewritten ReportReadRepository ledger query, /reports/history endpoint with X-Total-Count pagination and filters, entry-log DTO re-shape, ledger cutoff constant.
</objective>

<execution_context>
@C:/Users/felipecoelhosilva/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/felipecoelhosilva/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-visits-unification-backend/16-CONTEXT.md

# Prior-plan summaries — this plan consumes plan 16-01/16-02 outputs directly
@.planning/phases/16-visits-unification-backend/16-01-SUMMARY.md
@.planning/phases/16-visits-unification-backend/16-02-SUMMARY.md

# Analog files — read before editing
@src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/Persistence/ReportReadRepository.cs
@src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/Endpoints/ReportEndpoints.cs
@src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Abstractions/IReportReadRepository.cs
@src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Handlers/EntryLogHandlers.cs
@src/Modules/Photos/ControlEasyReborn.Modules.Photos.Api/Endpoints/EntryLogEndpoints.cs
@src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Time/TenantDayBoundary.cs

@AGENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): GET /api/v1/reports/history — one UNION, three branches, pagination, native stamps</name>
  <files>src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Contracts/ReportDtos.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Abstractions/IReportReadRepository.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Handlers/GetHistoryHandler.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/Persistence/ReportReadRepository.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/DI/ReportsModuleServiceCollectionExtensions.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/Endpoints/ReportEndpoints.cs, src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/DI/ReportEndpointsExtensions.cs, tests/ControlEasyReborn.IntegrationTests/ReportEndpointTests.cs</files>
  <read_first>
    - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Infrastructure/Persistence/ReportReadRepository.cs (the existing Visits + ConsentAuditLog read model — this is the file being rewritten; note its SelectRawAsync usage at lines 99-102 with explicit tenant predicates)
    - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Application/Time/TenantDayBoundary.cs (from plan 16-01 — the shared day-boundary helper for date-range normalization)
    - src/Modules/Reports/ControlEasyReborn.Modules.Reports.Api/Endpoints/ReportEndpoints.cs (MapGroup /api/v1/reports conventions, handler injection)
    - tests/ControlEasyReborn.IntegrationTests/ReportEndpointTests.cs (Testcontainers seed/assert pattern for report endpoints)
    - docker/mysql/init/14-visits-unification.sql (plan 16-01: AccessEvents.EventKind/PackageDescription/PackageCarrierCode columns this UNION reads)
  </read_first>
  <action>
    Per CONTEXT.md Unified Ledger Read Model (D-03) and VISIT-04/05:

    New contract in ReportDtos.cs: `HistoryRowResponse(Guid Id, string Kind, string NativeState, string Source, string SubjectType, string SubjectName, string? SubjectDocument, string? Purpose, string? DestinationLabel, string? PackageDescription, string? PackageCarrierCode, Guid? ApartmentId, DateTime OccurredAt, ...)` — Kind is the discriminator ("visit" / "access-event" / "legacy-entry-log"); NativeState is the row's OWN state vocabulary: VisitStatus.ToString() for visits, PolicyOutcome wire form for access-events, EntryState for legacy rows (never coerced into another enum's values — per D-03); Source is the origin label ("qr-scan" / "manual-lookup" / "walk-in" / "pre-registration" / "package-drop" / "security-event" / "consent-audit"); timestamps millisecond-precision (DATETIME(6) round-trip).

    In ReportReadRepository add `GetHistoryAsync(HistoryQuery query, CancellationToken)` building ONE `SelectRawAsync` SQL UNION with exactly three branches, each branch explicitly predicating `tenant_id = @p0` (per D-03: never let TenantFilterInterceptor splice into a UNION — set the raw-SQL flag/structure that keeps the interceptor out; the existing SelectRawAsync occupied-apartment query at lines 99-102 shows the established explicit-predicate pattern):
    - Branch 1 — Visits: columns mapped to the row shape, NativeState = Status (VisitStatus), Source from data (CheckedInAtUtc present + CreatedAtUtc same instant pattern is NOT reliable — add no new Visits columns; derive Source = "pre-registration" when CreatedAtUtc < CheckedInAtUtc and the visit was NOT created with CheckedIn status, else "walk-in"/"arrival" as derivable; keep this derivation simple and documented), Purpose, DestinationBlock/Unit, millisecond timestamps.
    - Branch 2 — AccessEvents: include visitor-access events AND refused-scan context — AccessEvents rows for visitor subjects with EventKind=Access and PolicyOutcome=Permit map with NativeState=PolicyOutcome wire ("permit"), Source from AccessMethod ("qr-scan"/"manual-lookup"); EventKind=PackageDrop rows map NativeState="permit", Source="package-drop", carrying PackageDescription/PackageCarrierCode (VISIT-06/07 surface, per D-04). DOUBLE-COUNT GUARD: only include AccessEvents rows whose ScanAttemptId did NOT already mutate/produce a Visit in the same cycle — the simplest deterministic rule per D-03 is to include visitor AccessEvents only when no Visits row with CheckedInAtUtc = OccurredAtUtc and same tenant exists (dedupe on timestamp+subject within the UNION via NOT EXISTS subquery on Visits by tenant_id and CheckedInAtUtc = ae.OccurredAtUtc); refused scans (RefusedScanAttempts, left as separate branch columns if simpler — allowed: refused rows may come from a fourth branch, still in the same UNION statement) map NativeState=failureCode wire form, never VisitStatus (per D-03/VISIT-05).
    - Branch 3 — ConsentAuditLog (legacy segment): only rows with RecordedAt < `LedgerCutoffUtc`, a `const DateTime LedgerCutoffUtc = new(2026, 9, 19, 0, 0, 0, DateTimeKind.Utc)` compile-time constant in ReportReadRepository (per D-02: build constant, no config). NativeState = EntryState string, Source = "consent-audit", SubjectName/Type from the audit columns, ApartmentLabel "Gatehouse" for gatehouse_only rows (preserving the existing MapConsentAuditEntry semantics at ReportReadRepository lines 271-321 as reference for field mapping).

    Order the merged result by timestamp DESC (millisecond precision), paginate server-side: LIMIT/OFFSET in SQL (not C# Skip), total count via a second COUNT over the same UNION (or COUNT(*) with identical WHERE over the UNION subquery). Handler `GetHistoryHandler` normalizes from/to via TenantDayBoundary (INFRA-02 day-boundary helper from plan 16-01) so "today" filters use tenant-local day boundaries.

    Endpoint: `GET /api/v1/reports/history` in ReportEndpoints — params page (default 1), pageSize (default 50, max 200), fromUtc, toUtc, status (optional), carrierCode (optional, server-side filter on branch 2 PackageCarrierCode exact match — VISIT-07), q (optional free-text matching PackageDescription / SubjectName / Purpose — VISIT-06), apartmentId (optional — apartment-bound package rows filterable by apartment, per D-04). Response: Results.Ok(rows) with header `X-Total-Count` set via the typed-results pattern (HttpContext.Response.Headers after handler returns total) — the header pattern Phase 17 consumes. Authorization: the group's existing RequireAuthorization stands (reports are read-role gated; keep consistent with /visit-counts-by-day which has no extra permission policy).

    Integration tests in ReportEndpointTests: seed via API calls (create visit + CheckInNow, package drop via /access-events/manual kind=package-drop) then assert /reports/history: rows include the visit (Kind=visit), the package row (Kind=access-event, Source=package-drop, PackageCarrierCode echoed), X-Total-Count equals seeded row count, page/pageSize slicing works, carrierCode filter returns only the package row, refused-scan seed appears with NativeState in failure-code form (NOT "CheckedIn"/"Pending" Visit vocabulary).
  </action>
  <verify>
    <automated>dotnet test tests/ControlEasyReborn.IntegrationTests --filter "FullyQualifiedName~ReportEndpointTests"</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/reports/history returns 200 with rows ordered by timestamp DESC, each row carrying Kind/NativeState/Source and X-Total-Count header set
    - Seeded visit + package drop + legacy (pre-cutoff) consent row all appear exactly once each — double-count guard verified: a visitor QR-scan arrival appears as exactly one row (Kind=visit), not one visit + one access-event
    - Refused scan appears with NativeState equal to its failure code string, never a VisitStatus string
    - carrierCode=<code> query param filters server-side (asserted by seeding two drops with different codes)
    - Rows with RecordedAt >= LedgerCutoffUtc (2026-09-19) do NOT appear in the legacy branch
    - Integration tests green via Testcontainers
  </acceptance_criteria>
  <done>The honest ledger stream works end-to-end: one UNION, three branches, native stamps, no double-counting, server-side pagination with X-Total-Count, carrier/apartment/description filters.</done>
</task>

<task type="auto">
  <name>Task 2: Entry-log re-scope — consent/audit contract only, walk-in fields removed</name>
  <files>src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Contracts/PhotoDtos.cs, src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Handlers/EntryLogHandlers.cs, src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Validators/EntryLogRequestValidator.cs, tests/ControlEasyReborn.IntegrationTests/EntryLogEndpointTests.cs</files>
  <read_first>
    - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Handlers/EntryLogHandlers.cs (CreateEntryLogHandler: ValidateStateTransitions incl. gatehouse_only/exited rules at lines 107-144 — the walk-in operational semantics being removed)
    - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Contracts/PhotoDtos.cs (CreateEntryLogRequest / EntryLogResponse at lines 23-41)
    - src/Modules/Photos/ControlEasyReborn.Modules.Photos.Application/Validators/EntryLogRequestValidator.cs
    - tests/ControlEasyReborn.IntegrationTests/EntryLogEndpointTests.cs (existing contract tests to update)
  </read_first>
  <action>
    Per CONTEXT.md Walk-in & Entry-Log Re-scope (D-02) and VISIT-02: POST /api/v1/entry-log keeps consent/audit writes ONLY. Remove walk-in operational semantics from the contract: `CreateEntryLogRequest` drops any walk-in fields (SubjectName/SubjectDocument remain as consent-audit subject identification — they are consent data, not operational walk-in fields; the DTO re-shape means: no destination, no visit-status, no operational entry concept beyond the consent EntryState). In `CreateEntryLogHandler.ValidateStateTransitions`, remove the `gatehouse_only` special case (it was the package-drop simulation — package drops now live in AccessEvents per plan 16-02; gatehouse_only rows are legacy-segment data, new writes of gatehouse_only are rejected with ProblemDetails ValidationException explaining the re-scope). Keep `exited`, `entered_with_consent`, `entered_override`, `entered_without_consent` (the privacy-consent role). Update `EntryLogRequestValidator` to reject `gatehouse_only` with a clear message. Update EntryLogEndpointTests: gatehouse_only POST now returns 400; consent-state POSTs unchanged and green. Do NOT touch the append-only triggers or existing ConsentAuditLog rows (no backfill mutation — per D-02/STATE.md; legacy rows stay read-only in the ledger below the cutoff from Task 1).

    Also update `GET /api/v1/dashboard/stats` (GetDashboardStatsHandler/ReportReadRepository lines 148-194): remove the ConsentAuditLog double-add in additionalTodayVisits/additionalOpenVisits — dashboard stats now count Visits only (the honest stream is /reports/history; per CONTEXT.md Integration Points the dashboard legacy consumer must stop double-counting). Keep RecentVisits populated from Visits only (drop MapConsentAuditEntry usage and the additionalOpenVisits/additionalTodayVisits additions, keep the counters themselves).
  </action>
  <verify>
    <automated>dotnet test tests/ControlEasyReborn.IntegrationTests --filter "FullyQualifiedName~EntryLogEndpointTests|FullyQualifiedName~ReportEndpointTests"</automated>
  </verify>
  <acceptance_criteria>
    - CreateEntryLogRequest no longer accepts gatehouse_only: POST with EntryState=gatehouse_only returns 400 ProblemDetails (integration assertion)
    - Consent states (entered_with_consent with photo, entered_override with reason, entered_without_consent, exited) still write ConsentAuditLog and return 201
    - DashboardStatsResponse.OpenVisits/TodayVisits no longer include ConsentAuditLog rows (integration test: seed a legacy consent row + a visit → OpenVisits counts visits only)
    - Existing append-only triggers untouched (no migration in this plan)
    - dotnet build src/ControlEasyReborn.sln exits 0
  </acceptance_criteria>
  <done>POST /api/v1/entry-log writes consent/audit only; walk-in operational records live solely in Visits; dashboard stops double-counting ConsentAuditLog rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP query params → /reports/history (page, pageSize, from/to, carrierCode, q, apartmentId) | Untrusted filter input reaches SQL construction |
| JWT tenant claim → UNION branches | Tenant isolation must hold per branch without interceptor help |
| Legacy ConsentAuditLog read path | Append-only historical data must not be mutated or newly exposed beyond its privacy-consent role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03-01 | Tampering (SQL injection) | ReportReadRepository.GetHistoryAsync dynamic WHERE/LIMIT construction | high | mitigate | All filter values parameterized (@paramN via SelectRawAsync parameters array — established pattern at lines 99-102); pageSize clamped server-side (max 200); identifiers (table/column names) are compile-time constants, never user input |
| T-16-03-02 | Info disclosure (cross-tenant) | UNION branches | high | mitigate | Explicit `tenant_id = @p0` predicate per branch (per D-03); integration test asserts tenant B's seeded rows never appear in tenant A's ledger response |
| T-16-03-03 | Info disclosure | Legacy consent rows in ledger | medium | mitigate | Legacy segment bounded by compile-time cutoff and read-only; rows carry consent-state stamps as historical context; ledger endpoint behind RequireAuthorization (same as existing report endpoints) |
| T-16-03-04 | Repudiation | History read access | low | accept | Read-only endpoint; existing Serilog request logging covers access trail |
| T-16-03-05 | DoS (resource) | Unbounded ledger queries | medium | mitigate | Server-side LIMIT/OFFSET with pageSize max 200; COUNT query bounded by the same WHERE; no N+1 (single UNION + single COUNT per request) |
| T-16-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; gate not triggered |
</threat_model>

<verification>
- `dotnet build src/ControlEasyReborn.sln` zero warnings
- Full gate per AGENTS.md: `scripts/verify-ci-local.sh` (or .ps1) passing at end of all tasks — includes Testcontainers integration + OpenAPI drift stage (new endpoint shapes must regenerate the OpenAPI client; run `npm run openapi-check` in the web project as stage 6 expects)
- Integration assertions: single-row arrival (no double count), X-Total-Count, carrierCode filter, cutoff exclusion, cross-tenant isolation
</verification>

<success_criteria>
- One chronological ledger stream at /api/v1/reports/history: Visits + visitor AccessEvents + legacy entry-log cutoff segment, each real-world entry counted exactly once
- Per-row kind discriminator, native state stamp, source origin; refused scans never coerced into Visit enums
- Server-side pagination (page/pageSize + X-Total-Count), carrierCode/description/apartment filters server-side
- Legacy walk-in segment read-only below the build-constant cutoff
- POST /api/v1/entry-log writes consent/audit only; dashboard stats no longer double-count
</success_criteria>

<output>
Create `.planning/phases/16-visits-unification-backend/16-03-SUMMARY.md` when done
</output>