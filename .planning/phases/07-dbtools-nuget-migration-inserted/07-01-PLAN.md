---
phase: 07
plan: 01
title: DBTools NuGet swap
wave: 1
depends_on: []
spec: .specs/dbtools-nuget-migration/
---

# Plan 07-01: DBTools NuGet Swap

## Objective

Replace vendored `src/lib/DBTools_SQL/` with NuGet [DBTools 1.4.3](https://www.nuget.org/packages/DBTools); verify build, tests, and Docker API health unchanged.

## Tasks

### Wave 1
1. Pin `DBTools` 1.4.3 in `src/Directory.Packages.props`
2. Swap `ControlEasyReborn.Infrastructure.csproj` to `<PackageReference Include="DBTools" />`

### Wave 2
3. Remove `src/lib/DBTools_SQL/` and solution entries
4. Remove vendored COPY from `docker/api.Dockerfile`

### Wave 3
5. `dotnet build` + `dotnet test` (including TenantFilterInterceptor tests)
6. Docker compose build + API health smoke

### Wave 4
7. Update ADR 0002, AGENTS.md, `.planning/codebase/STACK.md`, `CONCERNS.md`

## Verification

- No `src/lib/DBTools_SQL/` in repo
- `git grep -i "lib/DBTools" src/ docker/` returns nothing
- All tests green; API container healthy

## Risks

- `IQueryInterceptor` API drift between vendored copy and NuGet 1.4.3 — mitigated by running integration tests before deleting vendor
